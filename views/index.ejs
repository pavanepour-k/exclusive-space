<!DOCTYPE html>
<html lang="<%= lang %>">
<head>
  <%- include('partials/head') %>
</head>
<body class="min-h-screen flex flex-col">
  <%- include('partials/nav') %>
  <main class="container mx-auto p-4 flex-1 text-center space-y-4">
    <h1 class="text-4xl font-bold"><%= t('title') %></h1>
    <p><%= t('hero_tagline') %></p>
    <div class="space-x-4">
      <a href="/login" class="text-blue-600 underline"><%= t('cta_login') %></a>
      <a href="/register" class="text-blue-600 underline"><%= t('cta_register') %></a>
    </div>
  </main>
</body>
</html>